# Legacy Documentation

These files document the previous WAT architecture (Python tools + workflow layers) that predates the
current system (Trigger.dev TypeScript tasks + a scheduled Claude Code orchestrator). Kept for
historical reference only — nothing in here is loaded by the daily run.
